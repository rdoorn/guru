name = "find-symbol"
fixture = "flaskish"
prompt = "Where is save_upload defined and who calls it?"
timeout_s = 120
tags = ["search", "fast"]

[expect.behaviour]
# find_symbol/outline are the audited verbs for this question; run 69bc45821a7d
# answered correctly with find_symbol alone.
tools_used_any = ["search_code", "read_file", "find_symbol", "outline"]

[expect.content]
answer_contains = ["upload.py", "handlers.py"]
files_changed = []
